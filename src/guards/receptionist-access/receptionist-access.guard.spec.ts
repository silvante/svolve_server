import { ReceptionistAccessGuard } from './receptionist-access.guard';

describe('ReceptionistAccessGuard', () => {
  it('should be defined', () => {
    expect(new ReceptionistAccessGuard()).toBeDefined();
  });
});
